<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/united_kingdom_flag"
        android:label="@string/app_name"
        android:roundIcon="@drawable/uk_round_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.FastestLap"
        tools:targetApi="31">
        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.home.HomePageActivity" />
        <activity android:name=".ui.event.UpcomingEventsActivity" />
        <activity android:name=".ui.standing.ConstructorsStandingActivity" />
        <activity android:name=".ui.standing.DriversStandingActivity" />
        <activity android:name=".ui.event.EventActivity" />
        <activity android:name=".ui.event.PastEventsActivity" />
        <activity android:name=".ui.bio.TrackBioActivity" />
        <activity android:name=".ui.bio.ConstructorBioActivity" />
        <activity android:name=".ui.bio.DriverBioActivity" />
        <activity android:name=".ui.user.UserActivity" />
    </application>

</manifest>