<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Sfondo trasparente -->
    <stroke
        android:width="2dp"
        android:color="@color/sfondo_prova">
    </stroke> <!-- Spessore del bordo -->
         <!-- Colore del bordo -->
    <corners android:radius="1000dp" /> <!-- Arrotondamento degli angoli (opzionale) -->
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp" /> <!-- Spaziatura interna (opzionale) -->
</shape>